pipeline
    agent {
        label 'default'
    }
    stages{
        stage('Git Checkout'){
            steps{
                git 'https://github.com/subash211/subash.git'
            }
        }
        stage("set env variabl"){
            steps{
                sh 'export AWS_PROFILE=subash'
            }
        }
        stage('Get Directory') {
            steps{
                println(WORKSPACE)
            }
        }
        stage('Terraform init'){
            steps{
                sh 'terraform init'
            }
        }
        stage('Terraform Apply'){
            steps{
                withCredentials([[
                    $class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: "subash",
                    accessKeyVariable: 'AWS_ACCESS_KEY_ID',
                    secretKeyVariable: 'AWS_SECRET_ACCESS_KEY']]) {
                sh 'terraform apply --auto-approve'
                }
            }
        }
    }
}
